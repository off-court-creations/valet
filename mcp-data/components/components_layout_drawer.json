{
  "name": "Drawer",
  "category": "layout",
  "slug": "components/layout/drawer",
  "summary": "Drawer component",
  "aliases": [
    "drawer",
    "sidenav",
    "side-nav",
    "sidebar"
  ],
  "usage": {
    "purpose": "Off‑canvas panel for navigation and contextual tools.",
    "whenToUse": [
      "Secondary navigation, filters, or details alongside primary content",
      "Responsive navigation that collapses on small screens",
      "Non‑blocking utilities that can be dismissed"
    ],
    "whenNotToUse": [
      "Critical blocking decisions (use Modal)",
      "Transient feedback (use Snackbar)"
    ],
    "alternatives": [
      "Modal",
      "AppBar",
      "Panel"
    ]
  },
  "props": [
    {
      "name": "open",
      "type": "boolean",
      "required": true
    },
    {
      "name": "defaultOpen",
      "type": "boolean",
      "required": true,
      "default": "false"
    },
    {
      "name": "anchor",
      "type": "DrawerAnchor",
      "required": true,
      "default": "'left'"
    },
    {
      "name": "onClose",
      "type": "() => void",
      "required": true
    },
    {
      "name": "size",
      "type": "number | string",
      "required": true,
      "default": "'16rem'"
    },
    {
      "name": "disableBackdropClick",
      "type": "boolean",
      "required": true,
      "default": "false"
    },
    {
      "name": "disableEscapeKeyDown",
      "type": "boolean",
      "required": true,
      "default": "false"
    },
    {
      "name": "persistent",
      "type": "boolean",
      "required": true,
      "default": "false"
    },
    {
      "name": "adaptive",
      "type": "boolean",
      "required": true,
      "default": "false"
    },
    {
      "name": "toggleIcon",
      "type": "string",
      "required": true,
      "default": "'mdi:menu'"
    },
    {
      "name": "closeIcon",
      "type": "string",
      "required": true,
      "default": "'mdi:close'"
    },
    {
      "name": "children",
      "type": "React.ReactNode",
      "required": true
    }
  ],
  "cssVars": [
    "--valet-font-heading",
    "--valet-font-body",
    "--valet-font-mono",
    "--valet-font-button"
  ],
  "cssPresets": [],
  "events": [
    {
      "name": "onClose"
    }
  ],
  "actions": [],
  "slots": [
    {
      "name": "children"
    }
  ],
  "bestPractices": [
    "Choose the right mode. Use `persistent` for primary navigation; use overlay drawers for transient tasks. Prefer `adaptive` so navigation docks in landscape and becomes a toggle in portrait.",
    "Respect close affordances. Provide `onClose` for controlled drawers and keep backdrop/ESC enabled unless there is a strong reason to disable them.",
    "Size with tokens. Set `size` in `rem` or via spacing so density changes keep proportions; avoid content-driven widths that cause reflow thrash.",
    "Don’t manually offset for the AppBar. The drawer reads the current surface offset and adjusts itself (and persistent margins) automatically.",
    "Keep content focused and scrollable. Put navigation and short lists inside; for long collections consider windowing/virtualization and offload heavy work from the drawer."
  ],
  "bestPracticeSlugs": [
    "choose-the-right-mode-use-for-primary-navigation-use-overlay-drawers-for",
    "respect-close-affordances-provide-for-controlled-drawers-and-keep",
    "size-with-tokens-set-in-or-via-spacing-so-density-changes-keep",
    "dont-manually-offset-for-the-appbar-the-drawer-reads-the-current-surface",
    "keep-content-focused-and-scrollable-put-navigation-and-short-lists"
  ],
  "examples": [],
  "docsUrl": "/components/layout/DrawerDemo",
  "sourceFiles": [
    "src/components/layout/Drawer.tsx",
    "docs/src/pages/components/layout/DrawerDemo.tsx"
  ],
  "version": "0.30.6",
  "schemaVersion": "1.4"
}