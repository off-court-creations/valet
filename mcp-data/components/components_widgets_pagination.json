{
  "name": "Pagination",
  "category": "widgets",
  "slug": "components/widgets/pagination",
  "summary": "Tab-style page navigator with an elastic active-page underline; controlled by `count` + `page` + `onPageChange` — use it to page through large lists or tables.",
  "status": "experimental",
  "aliases": [
    "pagination",
    "pager",
    "paginator"
  ],
  "props": [
    {
      "name": "count",
      "type": "number",
      "required": true,
      "description": "Total number of pages (≥ 1)."
    },
    {
      "name": "page",
      "type": "number",
      "required": false,
      "description": "Currently-selected page (1-based).",
      "default": "1"
    },
    {
      "name": "onPageChange",
      "type": "(page: number) => void",
      "required": false,
      "description": "Called with the **new page** (1-based) when the user navigates."
    },
    {
      "name": "visibleWindow",
      "type": "number",
      "required": false,
      "description": "Limit how many page buttons are visible at once. When set and less than `count`,\npagination renders a sliding window of page numbers plus window scroll controls.\nDoes not affect the currently selected page."
    },
    {
      "name": "autoFollowActive",
      "type": "boolean",
      "required": false,
      "description": "If true, forces the active page to remain visible by auto-shifting the window\nwhenever `page` changes outside the current window. Defaults to true.",
      "default": "true"
    },
    {
      "name": "labels",
      "type": "DeepPartialStrings<ValetStrings['pagination']>",
      "required": false,
      "description": "Instance-level overrides for this component's i18n strings (nav root label,\nscroll/step button labels, per-page labels). Wins over the\n`ValetLocaleProvider` value, which in turn wins over the built-in English\ndefaults (A11Y S8 resolution contract; see `src/system/locale.tsx`)."
    },
    {
      "name": "sx",
      "type": "Sx",
      "required": false,
      "description": "Inline styles (with CSS var support)"
    },
    {
      "name": "preset",
      "type": "string | string[] | undefined",
      "required": false,
      "description": "One or many style-preset names registered via `definePreset()`"
    }
  ],
  "domPassthrough": {
    "element": "div",
    "omitted": [
      "onChange",
      "style"
    ]
  },
  "cssVars": [
    "--valet-pag-hit",
    "--valet-pag-vp-w",
    "--valet-pag-vp-h",
    "--valet-pag-track-x",
    "--valet-pag-x",
    "--valet-pag-w",
    "--valet-pag-trans-x",
    "--valet-pag-trans-w",
    "--valet-underline-width",
    "--valet-pag-scale",
    "--valet-pag-scale-trans"
  ],
  "cssPresets": [],
  "events": [
    {
      "name": "onPageChange",
      "payloadType": "number"
    }
  ],
  "slots": [],
  "bestPractices": [
    "Use `onPageChange` to receive the new 1-based page (`page` is controlled).",
    "Keep visible pages small. For large `count`, set `visibleWindow` (with `autoFollowActive`, default true) so only a window of page buttons shows plus window-scroll controls; rely on the prev/next controls for stepping.",
    "Targets are touch-friendly automatically — on coarse pointers each nav button floors at ≥44px tall (24px under `compact`); pick spacing/density rather than hand-sizing for mobile.",
    "Sync state when relevant. Mirror page in the router (query or path) so refresh/back behaves predictably.",
    "Prefer server‑side slicing for large datasets; ensure Table or List reflects the current page cleanly.",
    "Use accessible labels and titles for buttons (Prev/Next) and indicate current page to assistive tech.",
    "Consider infinite scroll for content feeds where paging is less meaningful.",
    "Align placement near the data it controls and avoid duplicating controls unless justified.",
    "Internationalize the built-in labels with the `labels` prop (instance-level) or a `ValetLocaleProvider` (app-wide). Resolution is instance `labels` > provider strings > built-in English."
  ],
  "examples": [
    {
      "id": "pagination-controlled",
      "title": "Controlled pagination",
      "code": "() => {\n  const [page, setPage] = React.useState(1);\n  return (<Stack>\n    <Pagination count={12} page={page} onPageChange={setPage} />\n    <Typography>Current page: {page}</Typography>\n  </Stack>);\n}",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {
        "count": 0
      }
    },
    {
      "id": "pagination-window",
      "title": "Visible window + autoFollow",
      "code": "() => {\n  const [page, setPage] = React.useState(5);\n  const [follow, setFollow] = React.useState(true);\n  return (<Stack>\n    <Pagination count={50} visibleWindow={7} autoFollowActive={follow} page={page} onPageChange={setPage} />\n    <Stack direction='row' sx={{ alignItems: 'center' }}>\n      <Typography variant='subtitle'>autoFollowActive</Typography>\n      <Switch checked={follow} onValueChange={(next) => setFollow(next)} aria-label='toggle autoFollowActive' />\n    </Stack>\n  </Stack>);\n}",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {
        "count": 0
      }
    }
  ],
  "docsUrl": "/pagination-demo",
  "sourceFiles": [
    "src/components/widgets/Pagination.tsx",
    "docs/src/pages/components/widgets/PaginationDemo.tsx"
  ],
  "version": "0.36.0",
  "schemaVersion": "1.7"
}