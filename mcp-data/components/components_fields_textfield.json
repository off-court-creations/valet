{
  "name": "TextField",
  "category": "fields",
  "slug": "components/fields/textfield",
  "summary": "TextField component",
  "status": "stable",
  "aliases": [
    "textfield",
    "textinput",
    "input",
    "textbox"
  ],
  "props": [
    {
      "name": "name",
      "type": "string",
      "required": false,
      "description": "Field identifier used for FormControl binding and forwarded to the underlying control\nwhen applicable. Components can require `name` locally if they depend on it."
    },
    {
      "name": "label",
      "type": "React.ReactNode",
      "required": false,
      "description": "Visual label rendered by the component or its wrapper. Accepts string or node."
    },
    {
      "name": "helperText",
      "type": "React.ReactNode",
      "required": false,
      "description": "Supplemental hint or validation content typically rendered under the control."
    },
    {
      "name": "error",
      "type": "boolean | undefined",
      "required": false,
      "description": "Marks the field invalid; components may adjust visuals and helper text color."
    },
    {
      "name": "fullWidth",
      "type": "boolean | undefined",
      "required": false,
      "description": "Stretches the field wrapper to occupy the full available width."
    },
    {
      "name": "sx",
      "type": "Sx | undefined",
      "required": false,
      "description": "Inline styles (with CSS var support)."
    },
    {
      "name": "preset",
      "type": "string | string[] | undefined",
      "required": false,
      "description": "One or many style-preset names registered via `definePreset()`"
    },
    {
      "name": "fontFamily",
      "type": "string | undefined",
      "required": false,
      "description": "Override input font"
    },
    {
      "name": "as",
      "type": "\"input\" | undefined | \"textarea\"",
      "required": false
    }
  ],
  "cssVars": [
    "--valet-focus-width",
    "--valet-focus-offset"
  ],
  "cssPresets": [],
  "events": [],
  "actions": [],
  "slots": [
    {
      "name": "label"
    },
    {
      "name": "helperText"
    }
  ],
  "bestPractices": [
    "Always provide a clear `label`. Avoid placeholder‑only fields; labels remain visible and accessible.",
    "Choose the correct `type` (e.g., `email`, `password`, `number`) and set `autoComplete` to improve UX and a11y.",
    "Pick a control model deliberately: use `value` + `onChange` for app‑state control; use `defaultValue` for simple local inputs.",
    "In forms, set a stable `name` so values submit predictably and validation maps cleanly.",
    "Use `as='textarea'` with `rows` for multi‑line input; keep width comfortable and avoid forcing long single‑line fields.",
    "Convey issues via `error` + `helperText`. Keep helper text succinct; reserve errors for actionable feedback.",
    "Use `fullWidth` in narrow or linear layouts; otherwise size with `sx` or parent layout for consistency.",
    "Prefer theme tokens and `preset` over ad‑hoc CSS to keep density and colours consistent."
  ],
  "examples": [
    {
      "id": "textfield-basic-auth",
      "title": "Email + Password",
      "code": "<Stack gap={1}>\n  <TextField name='email' type='email' label='Email' placeholder='you@example.com' autoComplete='email' />\n  <TextField name='password' type='password' label='Password' autoComplete='new-password' />\n</Stack>",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {}
    },
    {
      "id": "textfield-textarea",
      "title": "Multiline with helper",
      "code": "<Stack gap={1}>\n  <TextField name='bio' as='textarea' rows={4} label='Bio' placeholder='Tell us about yourself' helperText='Use 2–3 sentences.' />\n</Stack>",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {}
    }
  ],
  "sourceFiles": [
    "src/components/fields/TextField.tsx"
  ],
  "version": "0.32.0",
  "schemaVersion": "1.6"
}