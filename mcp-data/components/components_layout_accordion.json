{
  "name": "Accordion",
  "category": "layout",
  "slug": "components/layout/accordion",
  "summary": "Vertical stack of collapsible `Accordion.Item` panels with single or `multiple` open behavior and roving-focus ARIA; control which panels are open with `expanded`/`defaultExpanded`/`onExpandedChange`.",
  "status": "stable",
  "aliases": [
    "accordion",
    "expander"
  ],
  "props": [
    {
      "name": "defaultExpanded",
      "type": "number | number[]",
      "required": false,
      "description": "Uncontrolled seed: index (or indices) expanded on first render."
    },
    {
      "name": "expanded",
      "type": "number | number[]",
      "required": false,
      "description": "Controlled expanded index (or indices)."
    },
    {
      "name": "multiple",
      "type": "boolean",
      "required": false,
      "default": "false"
    },
    {
      "name": "onExpandedChange",
      "type": "(expanded: number[]) => void",
      "required": false,
      "description": "Fires with the next expanded indices whenever the user toggles a panel."
    },
    {
      "name": "headingLevel",
      "type": "1 | 2 | 3 | 4 | 5 | 6",
      "required": false,
      "default": "3"
    },
    {
      "name": "constrainHeight",
      "type": "boolean",
      "required": false,
      "default": "true"
    },
    {
      "name": "unmountOnExit",
      "type": "boolean",
      "required": false,
      "default": "false"
    },
    {
      "name": "sx",
      "type": "Sx",
      "required": false,
      "description": "Inline styles via CSSProperties (with CSS var support)"
    },
    {
      "name": "preset",
      "type": "string | string[] | undefined",
      "required": false,
      "description": "One or many style-preset names registered via `definePreset()`"
    },
    {
      "name": "compact",
      "type": "boolean | undefined",
      "required": false,
      "description": "Zeros all layout spacing (pad, gap, and spacing-margins) where supported and\ncascades to spacing-aware descendants. Does not touch control insets,\nstructural geometry, border-radius, glyph sizes, or alignment. `compact={false}`\nopts a subtree back out of an inherited compact."
    },
    {
      "name": "pad",
      "type": "Space | undefined",
      "required": false,
      "description": "Container padding as units or CSS length."
    }
  ],
  "domPassthrough": {
    "element": "div",
    "omitted": [
      "onChange"
    ]
  },
  "cssVars": [
    "--valet-acc-duration",
    "--valet-acc-ease",
    "--valet-divider-stroke",
    "--valet-acc-hit",
    "--valet-focus-width",
    "--valet-focus-ring-color",
    "--valet-focus-offset"
  ],
  "cssPresets": [],
  "events": [
    {
      "name": "onExpandedChange",
      "payloadType": "number[]"
    }
  ],
  "slots": [],
  "bestPractices": [
    "Use for short, scannable sections. Keep headers concise and content lightweight.",
    "Choose single vs multiple expansion by context; avoid opening many sections by default.",
    "Control model deliberately: use `expanded` + `onExpandedChange` for app‑driven state, or `defaultExpanded` for uncontrolled.",
    "Respect heading levels. Set `headingLevel` so the outline remains semantic within the page.",
    "Constrain height in dense pages so the thread scrolls locally rather than the document.",
    "Prefer keeping content mounted unless heavy: use `unmountOnExit` to remove expensive trees when closed.",
    "Ensure keyboard support: Space/Enter toggles, Arrow keys move focus, and focus styles are visible.",
    "Headers are touch-friendly automatically — the full-width header row floors at ≥44px tall on coarse pointers (40px under `compact`), with the mobile chrome kit (no tap-flash, no accidental text selection). Keep header text concise so the row stays a clean tap target.",
    "Each header is wired to its panel via `aria-controls`/`aria-labelledby` with ids namespaced per instance, so multiple Accordions on one page never produce colliding ARIA references."
  ],
  "examples": [
    {
      "id": "accordion-uncontrolled",
      "title": "Uncontrolled (single)",
      "code": "<Accordion>\n  <Accordion.Item header='Item 1'>Content</Accordion.Item>\n  <Accordion.Item header='Item 2'>Content</Accordion.Item>\n</Accordion>",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {}
    },
    {
      "id": "accordion-unmount-on-exit",
      "title": "Unmount on exit",
      "code": "<Accordion unmountOnExit>\n  <Accordion.Item header='Lazy'>Content mounts only when open</Accordion.Item>\n  <Accordion.Item header='Also lazy'>Content mounts only when open</Accordion.Item>\n</Accordion>",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {}
    },
    {
      "id": "accordion-controlled-multi",
      "title": "Controlled multiple",
      "code": "() => {\n  const [expanded, setExpanded] = React.useState([0,2]);\n  return (\n    <Accordion multiple expanded={expanded} onExpandedChange={setExpanded}>\n      <Accordion.Item header='Alpha'>A</Accordion.Item>\n      <Accordion.Item header='Bravo'>B</Accordion.Item>\n      <Accordion.Item header='Charlie'>C</Accordion.Item>\n    </Accordion>\n  );\n}",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {}
    }
  ],
  "docsUrl": "/accordion-demo",
  "sourceFiles": [
    "src/components/layout/Accordion.tsx",
    "docs/src/pages/components/layout/AccordionDemo.tsx"
  ],
  "version": "0.37.0",
  "schemaVersion": "1.7"
}