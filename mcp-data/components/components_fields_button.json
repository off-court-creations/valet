{
  "name": "Button",
  "category": "fields",
  "slug": "components/fields/button",
  "summary": "Theme-aware action button with `variant` (filled/outlined/plain), semantic `intent`, and `size`; reach for it for any primary or secondary click action, and use `IconButton` instead when the control is icon-only.",
  "status": "experimental",
  "aliases": [
    "button",
    "cta"
  ],
  "props": [
    {
      "name": "intent",
      "type": "Intent",
      "required": false,
      "description": "Semantic color intent (maps to theme tokens)."
    },
    {
      "name": "variant",
      "type": "ButtonVariant",
      "required": false,
      "description": "Visual variant: filled | outlined | plain."
    },
    {
      "name": "color",
      "type": "string",
      "required": false,
      "description": "Explicit color override (any CSS color or theme token name)."
    },
    {
      "name": "size",
      "type": "ButtonSize | number | string",
      "required": false
    },
    {
      "name": "fullWidth",
      "type": "boolean",
      "required": false
    },
    {
      "name": "sx",
      "type": "Sx",
      "required": false,
      "description": "Inline styles (with CSS var support)"
    },
    {
      "name": "preset",
      "type": "string | string[] | undefined",
      "required": false,
      "description": "One or many style-preset names registered via `definePreset()`"
    },
    {
      "name": "as",
      "type": "React.ElementType",
      "required": false,
      "description": "Polymorphic element override (renders this element/component instead of the default).",
      "default": "'button'"
    }
  ],
  "domPassthrough": {
    "element": "button",
    "omitted": []
  },
  "cssVars": [
    "--valet-bg",
    "--valet-text-color",
    "--valet-intent-bg",
    "--valet-intent-fg",
    "--valet-intent-border",
    "--valet-intent-focus",
    "--valet-intent-bg-hover",
    "--valet-intent-bg-active",
    "--valet-intent-fg-disabled"
  ],
  "cssPresets": [],
  "events": [],
  "slots": [],
  "bestPractices": [
    "Establish a clear action hierarchy: use `filled` (the default) for the primary action and `outlined` or `plain` for secondary/tertiary actions. Keep emphasis consistent within a view.",
    "Choose sizes by density: `sm` / `md` for typical forms, `lg` for standout CTAs. Avoid mixing sizes within the same control group.",
    "Use `variant` (`filled|outlined|plain`) and semantic `intent` to establish hierarchy. For custom palettes, use `color` (token name or CSS) — `filled` auto-contrasts the label.",
    "Buttons default to `type='button'`, so they will NOT submit a form. For the submit control inside a `<form>`, set `type='submit'` explicitly; use `type='reset'` to clear fields.",
    "Use `fullWidth` in narrow or linear layouts (mobile, drawers) where the button should span the container. Otherwise let content define width.",
    "For a single CTA inside a column/Stack, center using a parent container (`<Box alignX='center'>`) or `fullWidth` for stretch; avoid control-level centering.",
    "Prefer succinct verb‑first labels. If the action is icon‑only, use `IconButton` and provide an accessible label via `aria-label` .",
    "When combining icons and text, wrap them in a row layout (e.g., `<Stack direction='row' gap={theme.spacing(1)}>`) for readable spacing rather than relying on the label alone.",
    "Reuse styling with `preset` ( `definePreset()` ) rather than duplicating `sx` across instances.",
    "Avoid multiple primary actions per view and reserve destructive styles for destructive operations."
  ],
  "examples": [
    {
      "id": "button-centered-cta",
      "title": "Centered CTA in a column",
      "code": "<Stack gap={theme.spacing(1)}>\n  <Typography variant='subtitle'>Need approval?</Typography>\n  <Box alignX='center'><Button>Request review</Button></Box>\n</Stack>",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {}
    },
    {
      "id": "button-form-submit",
      "title": "Submit and reset buttons in a form",
      "code": "<form onSubmit={(e) => e.preventDefault()}>\n  <Stack direction='row' gap={theme.spacing(1)}>\n    <Button type='submit'>Save</Button>\n    <Button type='reset' variant='outlined'>Reset</Button>\n  </Stack>\n</form>",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {}
    }
  ],
  "docsUrl": "/button-demo",
  "sourceFiles": [
    "src/components/fields/Button.tsx",
    "docs/src/pages/components/field/ButtonDemoPage.tsx"
  ],
  "version": "0.36.0",
  "schemaVersion": "1.7"
}