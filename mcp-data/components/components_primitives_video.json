{
  "name": "Video",
  "category": "primitives",
  "slug": "components/primitives/video",
  "summary": "Video component",
  "status": "stable",
  "aliases": [
    "video",
    "player"
  ],
  "props": [
    {
      "name": "sources",
      "type": "VideoSource[]",
      "required": true,
      "description": "One or more video sources (MP4/WebM)."
    },
    {
      "name": "poster",
      "type": "string",
      "required": false,
      "description": "Poster image URL displayed before playback."
    },
    {
      "name": "tracks",
      "type": "VideoTrack[]",
      "required": false,
      "description": "Caption or subtitle tracks."
    },
    {
      "name": "controls",
      "type": "boolean",
      "required": false,
      "description": "Display native controls. Defaults to `true`.",
      "default": "true"
    },
    {
      "name": "autoPlay",
      "type": "boolean",
      "required": false,
      "description": "Autoplay when ready. Defaults to `true`.",
      "default": "true"
    },
    {
      "name": "muted",
      "type": "boolean",
      "required": false,
      "description": "Mute audio. Defaults to `true` for autoplay compliance.",
      "default": "true"
    },
    {
      "name": "loop",
      "type": "boolean",
      "required": false,
      "description": "Loop video.",
      "default": "false"
    },
    {
      "name": "width",
      "type": "string",
      "required": false,
      "description": "CSS width value, e.g. `100%` or `640px`."
    },
    {
      "name": "height",
      "type": "string",
      "required": false,
      "description": "CSS height value, e.g. `auto` or `360px`."
    },
    {
      "name": "lazy",
      "type": "boolean",
      "required": false,
      "description": "Lazy load when in viewport."
    },
    {
      "name": "objectFit",
      "type": "'contain' | 'cover'",
      "required": false,
      "description": "Custom CSS object-fit. Defaults to `contain`.",
      "default": "'contain'",
      "enumValues": [
        "contain",
        "cover"
      ]
    },
    {
      "name": "sx",
      "type": "Sx",
      "required": false,
      "description": "Inline styles (with CSS var support)"
    },
    {
      "name": "onPlay",
      "type": "(() => void) | undefined",
      "required": false
    },
    {
      "name": "onPause",
      "type": "(() => void) | undefined",
      "required": false
    },
    {
      "name": "onLoop",
      "type": "(() => void) | undefined",
      "required": false
    },
    {
      "name": "onError",
      "type": "((e: ErrorEvent) => void) | undefined",
      "required": false
    },
    {
      "name": "preset",
      "type": "string | string[] | undefined",
      "required": false,
      "description": "One or many style-preset names registered via `definePreset()`"
    }
  ],
  "domPassthrough": {
    "element": "div",
    "omitted": [
      "style",
      "children",
      "onError",
      "onPlay",
      "onPause"
    ]
  },
  "cssVars": [],
  "cssPresets": [],
  "events": [
    {
      "name": "onPlay"
    },
    {
      "name": "onPause"
    },
    {
      "name": "onLoop"
    },
    {
      "name": "onError",
      "payloadType": "ErrorEvent"
    }
  ],
  "actions": [],
  "slots": [],
  "bestPractices": [
    "Provide controls and captions; prefer muted autoplay only when appropriate.",
    "Use poster images for better perceived performance.",
    "Constrain size and avoid layout shift; select fit modes deliberately.",
    "Avoid background video for critical content; ensure text contrast remains accessible.",
    "Respect reduced motion preferences and keep transitions subtle.",
    "Lazy‑load non‑critical media and prefetch only when beneficial."
  ],
  "examples": [],
  "sourceFiles": [
    "src/components/primitives/Video.tsx"
  ],
  "version": "0.33.2",
  "schemaVersion": "1.6"
}