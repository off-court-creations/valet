{
  "name": "Image",
  "category": "primitives",
  "slug": "components/primitives/image",
  "summary": "Image component",
  "aliases": [
    "image",
    "img",
    "picture"
  ],
  "props": [
    {
      "name": "src",
      "type": "string",
      "required": true
    },
    {
      "name": "width",
      "type": "number | string",
      "required": true
    },
    {
      "name": "height",
      "type": "number | string",
      "required": true
    },
    {
      "name": "objectFit",
      "type": "'cover' | 'contain' | 'fill' | 'none' | 'scale-down'",
      "required": true,
      "default": "'cover'",
      "enumValues": [
        "cover",
        "contain",
        "fill",
        "none",
        "scale-down"
      ]
    },
    {
      "name": "rounded",
      "type": "number | string",
      "required": true
    },
    {
      "name": "lazy",
      "type": "boolean",
      "required": true,
      "default": "false"
    },
    {
      "name": "placeholder",
      "type": "string",
      "required": true
    },
    {
      "name": "sx",
      "type": "Sx",
      "required": true
    }
  ],
  "cssVars": [],
  "cssPresets": [],
  "events": [],
  "actions": [],
  "slots": [],
  "bestPractices": [
    "Write meaningful `alt` text. Describe the content/purpose; use `alt=\"\"` for purely decorative images to avoid noise for screen readers.",
    "Reserve layout space. Provide `width` and `height` (or a styled container) to prevent cumulative layout shift when images load.",
    "Lazy‑load offscreen media. Enable `lazy` with a tiny `placeholder` for non‑critical images; avoid lazy‑loading above‑the‑fold hero images.",
    "Choose the right `objectFit` . Use `cover` for fills and `contain` for letterboxed media; provide a background colour for pleasant letterboxing.",
    "Prevent drag ghosts. The component disables dragging by default; only enable dragging when there is a genuine use‑case.",
    "Optimize assets. Serve appropriately sized and compressed images; avoid shipping multi‑MB originals for small containers."
  ],
  "bestPracticeSlugs": [
    "write-meaningful-text-describe-the-contentpurpose-use-for-purely",
    "reserve-layout-space-provide-and-or-a-styled-container-to-prevent",
    "lazyload-offscreen-media-enable-with-a-tiny-for-noncritical-images-avoid",
    "choose-the-right-use-for-fills-and-for-letterboxed-media-provide-a",
    "prevent-drag-ghosts-the-component-disables-dragging-by-default-only",
    "optimize-assets-serve-appropriately-sized-and-compressed-images-avoid"
  ],
  "examples": [],
  "docsUrl": "/components/primitives/ImageDemo",
  "sourceFiles": [
    "src/components/primitives/Image.tsx",
    "docs/src/pages/components/primitives/ImageDemo.tsx"
  ],
  "version": "0.30.5",
  "schemaVersion": "1.3"
}