{
  "name": "Image",
  "category": "primitives",
  "slug": "components/primitives/image",
  "summary": "Image component",
  "status": "experimental",
  "aliases": [
    "image",
    "img",
    "picture"
  ],
  "props": [
    {
      "name": "src",
      "type": "string",
      "required": true,
      "description": "Image source URL"
    },
    {
      "name": "width",
      "type": "number | string",
      "required": false,
      "description": "CSS width value (number ⇒ px)"
    },
    {
      "name": "height",
      "type": "number | string",
      "required": false,
      "description": "CSS height value (number ⇒ px)"
    },
    {
      "name": "objectFit",
      "type": "'cover' | 'contain' | 'fill' | 'none' | 'scale-down'",
      "required": false,
      "description": "Object-fit value",
      "enumValues": [
        "cover",
        "contain",
        "fill",
        "none",
        "scale-down"
      ]
    },
    {
      "name": "lazy",
      "type": "boolean",
      "required": false,
      "description": "Use native browser lazy-loading"
    },
    {
      "name": "placeholder",
      "type": "string",
      "required": false,
      "description": "Optional lightweight placeholder shown behind while loading"
    },
    {
      "name": "aspectRatio",
      "type": "number | string",
      "required": false,
      "description": "CSS aspect-ratio value (number ⇒ n/1)"
    },
    {
      "name": "sx",
      "type": "Sx",
      "required": false,
      "description": "Inline styles (with CSS var support)"
    },
    {
      "name": "preset",
      "type": "string | string[] | undefined",
      "required": false,
      "description": "One or many style-preset names registered via `definePreset()`"
    }
  ],
  "domPassthrough": {
    "element": "img",
    "omitted": [
      "width",
      "height",
      "style"
    ]
  },
  "cssVars": [],
  "cssPresets": [],
  "events": [],
  "actions": [],
  "slots": [],
  "bestPractices": [
    "Always set alt text; use empty alt for decorative images.",
    "Constrain sizes and choose object‑fit (`cover`/`contain`) deliberately to avoid layout shift.",
    "Use responsive sources or sizes where appropriate; avoid full‑resolution assets in small spaces.",
    "Lazy‑load below‑the‑fold images; preload critical hero imagery.",
    "Avoid background images for content; prefer `<Image>` so a11y tools can interpret it.",
    "Apply rounded corners via a wrapper (Panel/Box) with `overflow: hidden`; do not rely on per‑component props."
  ],
  "examples": [],
  "sourceFiles": [
    "src/components/primitives/Image.tsx"
  ],
  "version": "0.32.0",
  "schemaVersion": "1.6"
}