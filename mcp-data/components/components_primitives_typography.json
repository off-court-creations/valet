{
  "name": "Typography",
  "category": "primitives",
  "slug": "components/primitives/typography",
  "summary": "Typography component",
  "aliases": [
    "typography",
    "text",
    "heading",
    "title"
  ],
  "usage": {
    "purpose": "Accessible text primitives for headings, paragraphs, and labels.",
    "whenToUse": [
      "Render headings, body text, captions, and helper text",
      "Communicate hierarchy and emphasis via variants and weights"
    ],
    "whenNotToUse": [
      "Interactive controls or links (use Button, IconButton, or anchor elements)"
    ],
    "alternatives": [
      "Button",
      "Panel"
    ]
  },
  "props": [
    {
      "name": "variant",
      "type": "'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6' | 'body' | 'subtitle' | 'button'",
      "required": true,
      "default": "'body'",
      "description": "Typography style preset"
    },
    {
      "name": "bold",
      "type": "boolean",
      "required": true,
      "default": "false",
      "description": "Use font-weight 700"
    },
    {
      "name": "italic",
      "type": "boolean",
      "required": true,
      "default": "false",
      "description": "Use italic font style"
    },
    {
      "name": "centered",
      "type": "boolean",
      "required": true,
      "default": "false",
      "description": "Center-align text and element within flex/grid parents"
    },
    {
      "name": "noSelect",
      "type": "boolean",
      "required": true,
      "default": "false"
    },
    {
      "name": "fontSize",
      "type": "string",
      "required": true,
      "default": "'-'",
      "description": "Explicit CSS font-size"
    },
    {
      "name": "scale",
      "type": "number",
      "required": true,
      "default": "'-'",
      "description": "Multiply the final size (autoSize aware)"
    },
    {
      "name": "autoSize",
      "type": "boolean",
      "required": true,
      "default": "false",
      "description": "Resize to the current breakpoint"
    },
    {
      "name": "color",
      "type": "string",
      "required": true,
      "default": "'-'",
      "description": "Override text colour"
    },
    {
      "name": "family",
      "type": "'heading' | 'body' | 'mono' | 'button'",
      "required": true,
      "enumValues": [
        "heading",
        "body",
        "mono",
        "button"
      ],
      "default": "'-'",
      "description": "Select a theme font family"
    },
    {
      "name": "fontFamily",
      "type": "string",
      "required": true,
      "default": "'-'",
      "description": "Override theme font family"
    },
    {
      "name": "whitespace",
      "type": "'normal' | 'pre' | 'pre-wrap' | 'pre-line'",
      "required": true,
      "default": "'normal'",
      "enumValues": [
        "normal",
        "pre",
        "pre-wrap",
        "pre-line"
      ]
    },
    {
      "name": "sx",
      "type": "Sx",
      "required": true
    },
    {
      "name": "preset",
      "type": "string | string[]",
      "required": false,
      "default": "'-'",
      "description": "Apply style presets"
    }
  ],
  "cssVars": [
    "--valet-text-color",
    "--valet-font-"
  ],
  "cssPresets": [],
  "events": [],
  "actions": [],
  "slots": [],
  "bestPractices": [
    "Match semantics to variants. Heading variants render semantic `h1–h6` elements; keep heading levels hierarchical and avoid skipping from `h1` to `h4` , etc. Prefer a single `h1` per page view.",
    "Use tokens, not pixels. Prefer `autoSize` , `scale` , and theme typography tokens over hard‑coded `fontSize` so type adapts to density and breakpoints.",
    "Choose families from the theme. Use `family` (heading/body/mono/button) or define fonts via `useInitialTheme` ; only use `fontFamily` overrides for special cases.",
    "Maintain contrast. Set `color` via theme tokens or surrounding containers ( `Panel` ) to ensure accessible contrast in light/dark modes.",
    "Don’t fake interactivity. Avoid attaching click handlers to Typography to mimic buttons/links; wrap it in `Button` or `<a>` for correct semantics and focus behaviour.",
    "Whitespace control. Use `whitespace` and `noSelect` for code, labels, and UI text to keep wrapping and selection intentional."
  ],
  "bestPracticeSlugs": [
    "match-semantics-to-variants-heading-variants-render-semantic-elements",
    "use-tokens-not-pixels-prefer-and-theme-typography-tokens-over-hardcoded",
    "choose-families-from-the-theme-use-headingbodymonobutton-or-define-fonts",
    "maintain-contrast-set-via-theme-tokens-or-surrounding-containers-to",
    "dont-fake-interactivity-avoid-attaching-click-handlers-to-typography-to",
    "whitespace-control-use-and-for-code-labels-and-ui-text-to-keep-wrapping"
  ],
  "examples": [],
  "docsUrl": "/components/primitives/TypographyDemoPage",
  "sourceFiles": [
    "src/components/primitives/Typography.tsx",
    "docs/src/pages/components/primitives/TypographyDemoPage.tsx"
  ],
  "version": "0.30.6",
  "schemaVersion": "1.4"
}