{
  "name": "DateSelector",
  "category": "fields",
  "slug": "components/fields/dateselector",
  "summary": "Interactive month calendar for picking a single date or a `range`; emits/accepts timezone-safe ISO `YYYY-MM-DD` strings, with display localized via `locale` and `firstDayOfWeek` (the value contract stays ISO Latin digits).",
  "status": "experimental",
  "aliases": [
    "date",
    "datepicker",
    "daterange"
  ],
  "props": [
    {
      "name": "value",
      "type": "string | [string, string]",
      "required": false,
      "description": "Controlled value. Single-date mode: ISO YYYY-MM-DD string. Range mode: tuple [start, end]."
    },
    {
      "name": "defaultValue",
      "type": "string | [string, string]",
      "required": false,
      "description": "Default value for uncontrolled usage (same shape as `value`)."
    },
    {
      "name": "onValueChange",
      "type": "OnValueChange<string | [string, string]>",
      "required": false,
      "description": "Event trio"
    },
    {
      "name": "onValueCommit",
      "type": "OnValueCommit<string | [string, string]>",
      "required": false
    },
    {
      "name": "name",
      "type": "string",
      "required": false,
      "description": "FormControl field name (single-date mode only)."
    },
    {
      "name": "minDate",
      "type": "string",
      "required": false,
      "description": "Earliest selectable ISO date (YYYY-MM-DD)."
    },
    {
      "name": "maxDate",
      "type": "string",
      "required": false,
      "description": "Latest selectable ISO date (YYYY-MM-DD)."
    },
    {
      "name": "range",
      "type": "boolean",
      "required": false,
      "description": "Enable dual start/end selection mode.",
      "default": "false"
    },
    {
      "name": "sx",
      "type": "Sx",
      "required": false,
      "description": "Inline styles (with CSS var support)"
    },
    {
      "name": "density",
      "type": "'tight' | 'standard' | 'comfortable'",
      "required": false,
      "description": "Density scale for the calendar's own spacing and visual sizing. The small\n(formerly `compact`) visual mode is now `density='tight'`; density never\nzeroes layout.",
      "enumValues": [
        "tight",
        "standard",
        "comfortable"
      ]
    },
    {
      "name": "compact",
      "type": "boolean",
      "required": false,
      "description": "Layout-compact relay: zeroes inherited layout spacing and cascades to\ndescendants via context. No longer a density alias."
    },
    {
      "name": "locale",
      "type": "string",
      "required": false,
      "description": "BCP-47 locale for the DISPLAY of month names, weekday headers, and day\nnumbers (via native `Intl`). Defaults to `'en-US'`, preserving the prior\nEnglish / Sunday-start output exactly. The committed VALUE is always ISO\n'YYYY-MM-DD' with Latin digits regardless of locale (e.g. `'ar-EG'` shows\nArabic-Indic display digits while `onValueChange` still emits Latin ISO).",
      "default": "'en-US'"
    },
    {
      "name": "firstDayOfWeek",
      "type": "1 | 2 | 3 | 4 | 5 | 6 | 7",
      "required": false,
      "description": "First day of the calendar week as an ISO weekday number\n(**1 = Monday … 7 = Sunday**). When omitted, the locale's own first day is\nused (`en-US` → 7/Sunday, `de-DE` → 1/Monday), keeping the default behavior\nSunday-first for the `en-US` default."
    },
    {
      "name": "labels",
      "type": "DeepPartialStrings<ValetStrings['dateSelector']>",
      "required": false,
      "description": "Instance-level overrides for this component's i18n strings (the month/year\nnavigation aria-labels). Wins over the `ValetLocaleProvider` value, which\nin turn wins over the built-in English defaults (A11Y S8 resolution\ncontract; see `src/system/locale.tsx`). Month/weekday names are Intl's\ndomain (A11Y S10), driven by `locale`, not part of this string table."
    },
    {
      "name": "preset",
      "type": "string | string[] | undefined",
      "required": false,
      "description": "One or many style-preset names registered via `definePreset()`"
    }
  ],
  "domPassthrough": {
    "element": "div",
    "omitted": [
      "onChange",
      "style"
    ]
  },
  "cssVars": [
    "--valet-font-body",
    "--valet-font-weight",
    "--valet-font-tracking",
    "--valet-font-leading",
    "--valet-date-radius",
    "--valet-date-cell-radius",
    "--valet-text-color"
  ],
  "cssPresets": [],
  "events": [
    {
      "name": "onValueChange",
      "payloadType": "OnValueChange<string | [string, string]>"
    },
    {
      "name": "onValueCommit",
      "payloadType": "OnValueCommit<string | [string, string]>"
    }
  ],
  "slots": [],
  "bestPractices": [
    "Use ISO 'YYYY-MM-DD' strings for values; avoid locale-dependent parsing.",
    "Control the value in app state to persist selection across navigation.",
    "Constrain input with minDate/maxDate to prevent out-of-range selection.",
    "For ranges, keep [start, end] ordered and handle partial selection gracefully.",
    "Avoid timezone pitfalls: treat values as local dates to prevent off-by-one errors.",
    "Prefer density='tight' on small viewports for a denser calendar; use default/comfortable on larger layouts. (The standalone `compact` boolean only zeros layout spacing and no longer drives the calendar's visual size — that moved to the density tier.)",
    "Binding precedence (prop > form > internal). An explicit `value` prop always wins; otherwise, in single-date mode, a `name` inside a `FormControl` binds the ISO string to the store (seed the key in `createFormStore(initial)` so the initial selection is honoured); otherwise the picker is uncontrolled. The binding is latched at mount. Range mode binds the `[start, end]` tuple under the same `name`.",
    "Change source. `onValueChange`/`onValueCommit` report a truthful `info.source`: a day cell is selected by clicking, so the source is `'pointer'` rather than a blanket `'programmatic'`.",
    "Internationalize the built-in labels with the `labels` prop (instance-level) or a `ValetLocaleProvider` (app-wide). Resolution is instance `labels` > provider strings > built-in English.",
    "Localize the calendar DISPLAY (month names, weekday headers, day-number digits) with the `locale` prop (BCP-47, default 'en-US' which preserves the prior English/Sunday-start output). The committed VALUE is always ISO 'YYYY-MM-DD' with Latin digits regardless of locale — e.g. locale='ar-EG' shows Arabic-Indic day digits while onValueChange/onValueCommit still emit Latin ISO.",
    "By default the week starts on the locale's own first day (en-US → Sunday, de-DE → Monday). Override it explicitly with `firstDayOfWeek` (1 = Monday … 7 = Sunday) to force a fixed start regardless of locale."
  ],
  "examples": [
    {
      "id": "dateselector-controlled",
      "title": "Controlled single date",
      "code": "() => {\n  const [date, setDate] = React.useState('2026-06-12');\n  return (<Stack gap={1}>\n    <DateSelector value={date} minDate='2026-01-01' maxDate='2026-12-31' onValueChange={(next) => setDate(typeof next === 'string' ? next : next[0])} />\n    <Typography>Selected: {date}</Typography>\n  </Stack>);\n}",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {}
    }
  ],
  "docsUrl": "/dateselector-demo",
  "sourceFiles": [
    "src/components/fields/DateSelector.tsx",
    "docs/src/pages/components/field/DateSelectorDemo.tsx"
  ],
  "version": "0.36.0",
  "schemaVersion": "1.7"
}