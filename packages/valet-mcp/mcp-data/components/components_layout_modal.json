{
  "name": "Modal",
  "category": "layout",
  "slug": "components/layout/modal",
  "summary": "Centered focus-trapping dialog rendered above a backdrop, controlled by `open`/`onClose`; use it for confirmations and focused tasks that must interrupt the page — reach for `Drawer` when the panel should slide from an edge.",
  "status": "stable",
  "aliases": [
    "modal",
    "dialog"
  ],
  "props": [
    {
      "name": "open",
      "type": "boolean",
      "required": false,
      "description": "Controlled visiblity"
    },
    {
      "name": "defaultOpen",
      "type": "boolean",
      "required": false,
      "description": "Default for uncontrolled",
      "default": "false"
    },
    {
      "name": "onClose",
      "type": "() => void",
      "required": false,
      "description": "Callback fired when the user requests to close (backdrop / ESC)"
    },
    {
      "name": "variant",
      "type": "'dialog' | 'alert'",
      "required": false,
      "description": "Dialog semantics (default) or alertdialog semantics",
      "default": "'dialog'",
      "enumValues": [
        "dialog",
        "alert"
      ]
    },
    {
      "name": "title",
      "type": "ReactNode",
      "required": false,
      "description": "Dialog title – used for aria-labelledby when provided"
    },
    {
      "name": "children",
      "type": "ReactNode",
      "required": false,
      "description": "Main body content"
    },
    {
      "name": "actions",
      "type": "ReactNode",
      "required": false,
      "description": "Action buttons"
    },
    {
      "name": "pad",
      "type": "Space",
      "required": false,
      "description": "Container padding override"
    },
    {
      "name": "compact",
      "type": "boolean",
      "required": false,
      "description": "Compact removes container + internal paddings"
    },
    {
      "name": "disableBackdropClick",
      "type": "boolean",
      "required": false,
      "description": "Disable closing via backdrop click",
      "default": "false"
    },
    {
      "name": "disableEscapeKeyDown",
      "type": "boolean",
      "required": false,
      "description": "Disable closing via ESC key",
      "default": "false"
    },
    {
      "name": "maxWidth",
      "type": "number | string",
      "required": false,
      "description": "Max width when not fullWidth"
    },
    {
      "name": "fullWidth",
      "type": "boolean",
      "required": false,
      "description": "Stretch to full viewport width minus gutter",
      "default": "false"
    },
    {
      "name": "sx",
      "type": "Sx",
      "required": false,
      "description": "Inline styles (with CSS var support)."
    },
    {
      "name": "data-valet-component",
      "type": "string",
      "required": false,
      "description": "Override the root component-identity marker (defaults to `'Modal'`). A\ncomposite that roots on a Modal passes its own name so the DOM node\nidentifies as that component — the AI-proxy hook.",
      "default": "'Modal'"
    },
    {
      "name": "preset",
      "type": "string | string[] | undefined",
      "required": false,
      "description": "One or many style-preset names registered via `definePreset()`"
    }
  ],
  "domPassthrough": {
    "element": "div",
    "omitted": [
      "style",
      "title"
    ]
  },
  "cssVars": [
    "--valet-backdrop-bg",
    "--valet-modal-duration",
    "--valet-modal-easing",
    "--valet-modal-viewport-margin",
    "--valet-modal-radius"
  ],
  "cssPresets": [],
  "events": [
    {
      "name": "onClose"
    }
  ],
  "slots": [
    {
      "name": "title"
    },
    {
      "name": "children"
    },
    {
      "name": "actions"
    }
  ],
  "bestPractices": [
    "Use for critical decisions or blocking flows; avoid overusing for simple notifications (use `Snackbar`).",
    "Manage focus correctly: trap within the dialog and restore focus to the trigger on close.",
    "Provide clear primary/secondary actions; keep destructive actions explicit and separated.",
    "Adjust size to content: avoid scrollable modals within scrollable pages when possible.",
    "Prefer `Panel` for non‑modal grouping and background content separation.",
    "Respect reduced motion and keep transitions subtle and consistent with theme tokens."
  ],
  "examples": [],
  "docsUrl": "/modal-demo",
  "sourceFiles": [
    "src/components/layout/Modal.tsx",
    "docs/src/pages/components/layout/ModalDemo.tsx"
  ],
  "version": "0.37.0",
  "schemaVersion": "1.7"
}