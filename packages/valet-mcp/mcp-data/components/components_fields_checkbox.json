{
  "name": "Checkbox",
  "category": "fields",
  "slug": "components/fields/checkbox",
  "summary": "Accessible boolean checkbox for a single on/off choice; controlled via `checked` + `onValueChange`, or auto-bound to a `FormControl` with `name` (keep `bindForm` to opt a form-bound box out of writing the store).",
  "status": "stable",
  "aliases": [
    "checkbox"
  ],
  "props": [
    {
      "name": "name",
      "type": "string",
      "required": false,
      "description": "Field name used for FormControl binding and form submission. When `bindForm` is false,\n`name` may be omitted and no form binding/submission occurs."
    },
    {
      "name": "bindForm",
      "type": "boolean",
      "required": false,
      "description": "Disable FormControl binding and omit name from submission."
    },
    {
      "name": "checked",
      "type": "boolean",
      "required": false
    },
    {
      "name": "defaultChecked",
      "type": "boolean",
      "required": false
    },
    {
      "name": "size",
      "type": "CheckboxSize | number | string",
      "required": false,
      "description": "Visual size; token or CSS length."
    },
    {
      "name": "onChange",
      "type": "(event: ChangeEvent<HTMLInputElement>) => void",
      "required": false,
      "description": "DOM-parity change event (raw React event)."
    },
    {
      "name": "onValueChange",
      "type": "OnValueChange<boolean>",
      "required": false,
      "description": "Canonical value change event (fires on each mutation)."
    },
    {
      "name": "onValueCommit",
      "type": "OnValueCommit<boolean>",
      "required": false,
      "description": "Commit event (fires at the same moment for toggles)."
    },
    {
      "name": "indeterminate",
      "type": "boolean",
      "required": false,
      "description": "Mixed (indeterminate) visual state for parent selections."
    },
    {
      "name": "label",
      "type": "React.ReactNode",
      "required": false,
      "description": "Visual label rendered by the component or its wrapper. Accepts string or node."
    },
    {
      "name": "helperText",
      "type": "React.ReactNode",
      "required": false,
      "description": "Supplemental hint or validation content typically rendered under the control."
    },
    {
      "name": "error",
      "type": "boolean | undefined",
      "required": false,
      "description": "Marks the field invalid; components may adjust visuals and helper text color."
    },
    {
      "name": "fullWidth",
      "type": "boolean | undefined",
      "required": false,
      "description": "Stretches the field wrapper to occupy the full available width."
    },
    {
      "name": "sx",
      "type": "Sx | undefined",
      "required": false,
      "description": "Inline styles (with CSS var support)."
    },
    {
      "name": "preset",
      "type": "string | string[] | undefined",
      "required": false,
      "description": "One or many style-preset names registered via `definePreset()`"
    }
  ],
  "domPassthrough": {
    "element": "input",
    "omitted": [
      "type",
      "size",
      "onChange",
      "value",
      "defaultValue",
      "style",
      "name"
    ]
  },
  "cssVars": [
    "--valet-checkbox-radius",
    "--valet-checkbox-stroke",
    "--valet-checkbox-inset"
  ],
  "cssPresets": [],
  "events": [
    {
      "name": "onChange",
      "payloadType": "ChangeEvent<HTMLInputElement>"
    },
    {
      "name": "onValueChange",
      "payloadType": "OnValueChange<boolean>"
    },
    {
      "name": "onValueCommit",
      "payloadType": "OnValueCommit<boolean>"
    }
  ],
  "slots": [
    {
      "name": "label"
    },
    {
      "name": "helperText"
    }
  ],
  "bestPractices": [
    "Use Checkbox for independent booleans. For mutually exclusive choices, prefer `RadioGroup` .",
    "Always provide a clear label. The entire label should be clickable. In forms, set a stable `name` so values submit predictably.",
    "Choose control model deliberately. Use `checked` / `onChange` when the value participates in app state; otherwise `defaultChecked` is fine for local forms.",
    "Keep targets comfortable. Pick `size` tokens or numeric sizes that keep tap areas ≥ 40–48px in dense UIs.",
    "Group with a visible question or caption when multiple checkboxes relate to one concept; don’t rely on proximity alone.",
    "Disabled with context. If a checkbox is disabled, explain why nearby; avoid silently disabling without guidance.",
    "Tokens/presets. Style with theme tokens and `preset` rather than ad‑hoc CSS to keep density and branding consistent."
  ],
  "examples": [
    {
      "id": "checkbox-controlled",
      "title": "Controlled checkbox",
      "code": "() => {\n  const [agreed, setAgreed] = React.useState(false);\n  return (<Stack direction='row' gap={1}>\n    <Checkbox checked={agreed} onValueChange={(next) => setAgreed(next)} />\n    <Typography>I accept the terms</Typography>\n  </Stack>);\n}",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {}
    },
    {
      "id": "checkbox-form",
      "title": "FormControl binding",
      "code": "() => {\n  const useStore = Valet.createFormStore({ newsletter: true });\n  return (<FormControl useStore={useStore} onSubmitValues={(values) => void values}>\n    <Stack direction='row' gap={1}>\n      <Checkbox name='newsletter' />\n      <Typography>Subscribe to the newsletter</Typography>\n    </Stack>\n  </FormControl>);\n}",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {}
    }
  ],
  "docsUrl": "/checkbox-demo",
  "sourceFiles": [
    "src/components/fields/Checkbox.tsx",
    "docs/src/pages/components/field/CheckBoxDemo.tsx"
  ],
  "version": "0.36.0",
  "schemaVersion": "1.7"
}