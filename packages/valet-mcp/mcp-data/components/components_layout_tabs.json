{
  "name": "Tabs",
  "category": "layout",
  "slug": "components/layout/tabs",
  "summary": "Tabbed interface with `Tabs.Tab` triggers and `Tabs.Panel` content and full `tablist` ARIA; controlled via `value`/`onValueChange` — use it to switch between sibling views in the same region.",
  "status": "stable",
  "aliases": [
    "tabs"
  ],
  "props": [
    {
      "name": "value",
      "type": "string | number",
      "required": false
    },
    {
      "name": "defaultValue",
      "type": "string | number",
      "required": false
    },
    {
      "name": "onValueChange",
      "type": "OnValueChange<string | number>",
      "required": false,
      "description": "Canonical value trio (API-TYPES S4 / ruling R10): the inline lookalike\npayloads were replaced with OnValueChange/OnValueCommit,\nso `info` is now the full ChangeInfo — including the honest\n`source` (tab click ⇒ `'pointer'`, arrow-key nav ⇒ `'keyboard'`)."
    },
    {
      "name": "onValueCommit",
      "type": "OnValueCommit<string | number>",
      "required": false
    },
    {
      "name": "orientation",
      "type": "'horizontal' | 'vertical'",
      "required": false,
      "enumValues": [
        "horizontal",
        "vertical"
      ]
    },
    {
      "name": "placement",
      "type": "'top' | 'bottom' | 'left' | 'right'",
      "required": false,
      "enumValues": [
        "top",
        "bottom",
        "left",
        "right"
      ]
    },
    {
      "name": "alignX",
      "type": "'left' | 'right' | 'center'",
      "required": false,
      "description": "Horizontal alignment of the tab strip (horizontal orientation).",
      "enumValues": [
        "left",
        "right",
        "center"
      ]
    },
    {
      "name": "tabAlign",
      "type": "'left' | 'right' | 'center'",
      "required": false,
      "description": "Alias for alignX for clarity.",
      "enumValues": [
        "left",
        "right",
        "center"
      ]
    },
    {
      "name": "sx",
      "type": "Sx",
      "required": false,
      "description": "Inline styles (with CSS var support)"
    },
    {
      "name": "preset",
      "type": "string | string[] | undefined",
      "required": false,
      "description": "One or many style-preset names registered via `definePreset()`"
    },
    {
      "name": "compact",
      "type": "boolean | undefined",
      "required": false,
      "description": "Zeros all layout spacing (pad, gap, and spacing-margins) where supported and\ncascades to spacing-aware descendants. Does not touch control insets,\nstructural geometry, border-radius, glyph sizes, or alignment. `compact={false}`\nopts a subtree back out of an inherited compact."
    },
    {
      "name": "pad",
      "type": "Space | undefined",
      "required": false,
      "description": "Container padding as units or CSS length."
    },
    {
      "name": "gap",
      "type": "Space | undefined",
      "required": false,
      "description": "Inter-child spacing as units or CSS length."
    },
    {
      "name": "density",
      "type": "\"tight\" | \"standard\" | \"comfortable\" | undefined",
      "required": false,
      "description": "Density override for the spacing scale; defaults from nearest Surface."
    }
  ],
  "domPassthrough": {
    "element": "div",
    "omitted": [
      "onChange",
      "style"
    ]
  },
  "cssVars": [
    "--valet-focus-width",
    "--valet-focus-ring-color",
    "--valet-focus-offset",
    "--valet-underline-width",
    "--valet-space"
  ],
  "cssPresets": [],
  "events": [
    {
      "name": "onValueChange",
      "payloadType": "OnValueChange<string | number>"
    },
    {
      "name": "onValueCommit",
      "payloadType": "OnValueCommit<string | number>"
    }
  ],
  "slots": [],
  "bestPractices": [
    "Use Tabs to switch peer views that don’t require navigation changes; use routes for deep navigation.",
    "Keep labels short and stable; avoid wrapping to multiple rows—enable horizontal scroll when needed.",
    "Provide sensible initial selection and preserve it across renders/routing.",
    "Respect overflow: allow swipe/drag to scroll when the strip exceeds width; show edge fades to hint at more content.",
    "Ensure keyboard navigation: Arrow keys move focus, Enter/Space activates.",
    "Prefer tokenized spacing and motion for a cohesive feel.",
    "Avoid mixing tabs and inline navigation within the same strip.",
    "Use appropriate variants and placement to reflect app hierarchy.",
    "Announce active tab for screen readers: the strip exposes `role=tablist` with `aria-orientation` tracking the `orientation` prop, each tab carries `aria-selected` and `aria-controls`, and tab/panel ids are namespaced per instance so multiple Tabs on a page never collide.",
    "Prefer the canonical `onValueChange`/`onValueCommit` events: each fires with the full `ChangeInfo` payload (`previousValue`, `phase`, and an honest `info.source`) — a tab click reports `'pointer'` and Arrow-key navigation reports `'keyboard'`."
  ],
  "examples": [],
  "docsUrl": "/tabs-demo",
  "sourceFiles": [
    "src/components/layout/Tabs.tsx",
    "docs/src/pages/components/layout/TabsDemo.tsx"
  ],
  "version": "0.36.0",
  "schemaVersion": "1.7"
}