{
  "name": "Markdown",
  "category": "widgets",
  "slug": "components/widgets/markdown",
  "summary": "Renders a Markdown string into valet primitives (headings, lists, links, fenced code with highlighting); use it to display AI/CMS output as themed React, not raw HTML.",
  "status": "stable",
  "aliases": [
    "markdown",
    "md",
    "richtext"
  ],
  "props": [
    {
      "name": "data",
      "type": "string",
      "required": true,
      "description": "Raw markdown text"
    },
    {
      "name": "codeBackground",
      "type": "string",
      "required": false,
      "description": "Optional override for the code block surface (the whole code area, not just\nits padding ring). Defaults to a GitHub-paired surface (LIGHT_BG/DARK_BG)."
    },
    {
      "name": "noSelect",
      "type": "boolean",
      "required": false,
      "description": "Render the rendered prose non-selectable (`user-select: none`, cascades to\nall child elements). Default: text is selectable.",
      "default": "false"
    },
    {
      "name": "sx",
      "type": "Sx",
      "required": false,
      "description": "Inline styles (with CSS var support)"
    },
    {
      "name": "preset",
      "type": "string | string[] | undefined",
      "required": false,
      "description": "One or many style-preset names registered via `definePreset()`"
    }
  ],
  "domPassthrough": {
    "element": "div",
    "omitted": [
      "style"
    ]
  },
  "cssVars": [
    "--valet-focus-ring-color",
    "--valet-divider"
  ],
  "cssPresets": [],
  "events": [],
  "slots": [],
  "bestPractices": [
    "Keep input trusted or sanitized. Do not feed untrusted Markdown unless you sanitize it first.",
    "Prefer concise content. Split very long documents into sections with clear headings.",
    "Use fenced code blocks with a language hint for syntax highlighting.",
    "Raw HTML in the source is NOT rendered — it shows as inert escaped text (e.g. `<b>` appears literally). There is no dangerouslySetInnerHTML for content; this is the core XSS guarantee. Use valet components for rich layout instead of embedding HTML.",
    "`codeBackground` overrides the entire code-block surface (not just its padding ring). The default is a GitHub-paired surface — light `#f6f8fa` / dark `#0d1117` — chosen to match the bundled highlight.js GitHub syntax theme (which supplies the token colours); override it (e.g. to a chat bubble's colour) so code blends into its container.",
    "Use descriptive link text. External links (absolute http/https/ftp) automatically get `rel=\"noopener noreferrer\"`; link and image URLs are scheme-allowlisted and control-char/whitespace-obfuscated schemes are rejected. For internal SPA navigation, prefer router links or intercept anchors to avoid full page reloads.",
    "Maintain a proper heading hierarchy (h1 → h2 → h3). Keep one top-level title per page and use lower levels within Markdown.",
    "Always include meaningful alt text on images; keep images responsive and reasonably sized.",
    "Keep tables concise, include a header row, and use column alignment markers (:-, :-:, -:) for clarity.",
    "Treat GFM task list checkboxes as status indicators; don’t expect them to be interactive in rendered Markdown.",
    "For advanced code UX (copy/download, long snippets), use the dedicated CodeBlock widget instead of plain fenced blocks.",
    "If enabling raw HTML rendering in the future, run content through a robust sanitizer before passing to Markdown.",
    "Avoid huge images and extremely long single documents. Split content and load lazily when possible for performance."
  ],
  "examples": [
    {
      "id": "markdown-basic",
      "title": "Headings, emphasis, and code",
      "code": "<Markdown data={`# Title\\n\\nThis is **bold** and this is *italic*.\\n\\n\\`inline code\\` and a block:\\n\\n\\u0060\\u0060\\u0060ts\\nconst n: number = 42\\nconsole.log(n)\\n\\u0060\\u0060\\u0060`} />",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {
        "data": ""
      }
    },
    {
      "id": "markdown-table",
      "title": "Table example",
      "code": "<Markdown data={`| Fruit | Colour |\\n| ----- | ------ |\\n| Apple | Red    |\\n| Pear  | Green  |`} />",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {
        "data": ""
      }
    },
    {
      "id": "markdown-lists-tasks",
      "title": "Ordered lists and task lists",
      "code": "<Markdown data={`### Lists & Tasks\\n\\n- Item one\\n- Item two with **bold** and a [link](https://example.com)\\n\\n1. First\\n2. Second\\n3. Third\\n\\n- [ ] Unchecked task\\n- [x] Completed task\\n- [ ] Partially **formatted** task with \\`code\\``} />",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {
        "data": ""
      }
    },
    {
      "id": "markdown-quote-hr",
      "title": "Blockquote and horizontal rule",
      "code": "<Markdown data={`> Blockquote with nested **emphasis** and a link to [docs](https://react.dev).\\n>\\n> - Point A\\n> - Point B\\n\\n---\\n\\nSoft break below:  \\nSecond line (two spaces above)`} />",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {
        "data": ""
      }
    },
    {
      "id": "markdown-table-aligned",
      "title": "Table with alignment and inline formatting",
      "code": "<Markdown data={`| Feature | Support | Notes |\\n|:--------|:-------:|------:|\\n| Bold    |  yes    | **strong** |\\n| Code    |  yes    | \\`inline\\` & blocks |\\n| Links   |  yes    | [safe](https://example.com) |`} />",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {
        "data": ""
      }
    },
    {
      "id": "markdown-links-images",
      "title": "Links, mailto, and sanitized unsafe link; image",
      "code": "<Markdown data={`Safe: [archway](https://archway.design)\\n\\nMail: [email](mailto:test@example.com)\\n\\nUnsafe (sanitized): [do not click](javascript:alert(1))\\n\\n![Placeholder](https://placehold.co/160x80/png)`} />",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {
        "data": ""
      }
    },
    {
      "id": "markdown-code-fallback",
      "title": "Code highlighting and unknown language fallback",
      "code": "<Markdown data={`\\u0060\\u0060\\u0060ts\\nconst greet = (name: string) => 'Hello, ' + name;\\n\\u0060\\u0060\\u0060\\n\\n\\u0060\\u0060\\u0060brainfuck\\n>+[,.]  // unknown language; falls back to plaintext\\n\\u0060\\u0060\\u0060`} />",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {
        "data": ""
      }
    }
  ],
  "docsUrl": "/markdown-demo",
  "sourceFiles": [
    "src/components/widgets/Markdown.tsx",
    "docs/src/pages/components/widgets/MarkdownDemo.tsx"
  ],
  "version": "0.36.0",
  "schemaVersion": "1.7"
}