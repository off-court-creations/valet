{
  "name": "Table",
  "category": "widgets",
  "slug": "components/widgets/table",
  "summary": "Data table with sortable columns, smart height/auto-pagination, and unified `selectionMode` (`'none'|'single'|'multiple'`) keyed by `getItemKey` for selection that survives data refreshes (the `selectable`/`rowKey` props are deprecated aliases); use it for tabular data with columns.",
  "status": "stable",
  "aliases": [
    "table",
    "datagrid",
    "data-grid"
  ],
  "props": [
    {
      "name": "data",
      "type": "T[]",
      "required": true
    },
    {
      "name": "columns",
      "type": "TableColumn<T>[]",
      "required": true
    },
    {
      "name": "striped",
      "type": "boolean",
      "required": false
    },
    {
      "name": "hoverable",
      "type": "boolean",
      "required": false
    },
    {
      "name": "dividers",
      "type": "boolean",
      "required": false
    },
    {
      "name": "initialSort",
      "type": "{ index: number; desc?: boolean }",
      "required": false
    },
    {
      "name": "onSortChange",
      "type": "(index: number, desc: boolean) => void",
      "required": false
    },
    {
      "name": "onSelectionChange",
      "type": "(selected: T[]) => void",
      "required": false,
      "description": "Fired only on user interaction (checkbox toggle) and when pruning drops\nrows — the unified SelectionProps.onSelectionChange, emitting the\nselected **rows** (Table's selection unit is `T`)."
    },
    {
      "name": "onRowClick",
      "type": "(row: T, rowIndex: number, event: React.MouseEvent<HTMLTableRowElement>) => void",
      "required": false,
      "description": "Fired when a row is clicked (ignores clicks that originate from obvious interactive children\nlike buttons, inputs, or links). Receives the row object, its visible index, and the click event."
    },
    {
      "name": "constrainHeight",
      "type": "boolean",
      "required": false
    },
    {
      "name": "minConstrainedRows",
      "type": "number",
      "required": false,
      "description": "Minimum body rows that must be visible for an internal scrollbar to be used.\nIf the constrained viewport would show fewer rows than this, height constraining is disabled\nto avoid a tiny scroller. Default: 4."
    },
    {
      "name": "maxExpandedRows",
      "type": "number",
      "required": false,
      "description": "Applies only when `minConstrainedRows` disables the usual constraint. When the dataset\nexceeds this size, pagination kicks in (via <Pagination/>) and shows this many rows\nper page instead of using an internal scrollbar. Default: 30."
    },
    {
      "name": "paginate",
      "type": "boolean",
      "required": false,
      "description": "If true, always paginate using `maxExpandedRows` rows per page, regardless of\n`minConstrainedRows`/`constrainHeight`. When false (default), pagination engages\nautomatically only when `minConstrainedRows` disables constraint and the dataset\nexceeds `maxExpandedRows`."
    },
    {
      "name": "page",
      "type": "number",
      "required": false,
      "description": "Controlled current page (1-based) when `paginate` is true or auto-pagination engages.\nIf omitted, the table manages its own page state."
    },
    {
      "name": "onPageChange",
      "type": "(page: number) => void",
      "required": false,
      "description": "Called when the page changes (receives the 1-based page number). Used with controlled `page`."
    },
    {
      "name": "paginationWindow",
      "type": "number",
      "required": false,
      "description": "Controls the width of the visible window of page buttons when pagination renders.\nPassed to <Pagination visibleWindow>. Optional; defaults to Pagination’s internal behavior."
    },
    {
      "name": "sx",
      "type": "Sx",
      "required": false,
      "description": "Inline styles (with CSS var support)"
    },
    {
      "name": "preset",
      "type": "string | string[] | undefined",
      "required": false,
      "description": "One or many style-preset names registered via `definePreset()`"
    },
    {
      "name": "selectionMode",
      "type": "\"multiple\" | \"none\" | \"single\" | undefined",
      "required": false,
      "description": "How many items may be selected at once.\n- `'none'` — selection is disabled (the default for components that gate it).\n- `'single'` — at most one item is selected.\n- `'multiple'` — any number of items may be selected.\n\nCanonical replacement for the per-component flags (Table's `selectable`)."
    },
    {
      "name": "selected",
      "type": "T[] | undefined",
      "required": false,
      "description": "Controlled selection as an array of selection units (`K`)."
    },
    {
      "name": "defaultSelected",
      "type": "T[] | undefined",
      "required": false,
      "description": "Uncontrolled initial selection as an array of selection units (`K`)."
    },
    {
      "name": "getItemKey",
      "type": "keyof T | ((item: T, index: number) => string | number) | undefined",
      "required": false,
      "description": "Stable identity for each item — a property name or a function of the item\nand its index. Canonical, cross-component replacement for Table's `rowKey`\nand List's `getKey`. When omitted, components fall back to reference/index\nidentity exactly as before."
    }
  ],
  "domPassthrough": {
    "element": "table",
    "omitted": [
      "children",
      "style"
    ]
  },
  "cssVars": [
    "--valet-divider-stroke",
    "--valet-underline-width",
    "--valet-focus-width",
    "--valet-focus-offset",
    "--valet-bg",
    "--valet-table-underline"
  ],
  "cssPresets": [],
  "events": [
    {
      "name": "onSortChange",
      "payloadType": "number"
    },
    {
      "name": "onSelectionChange",
      "payloadType": "T[]"
    },
    {
      "name": "onRowClick",
      "payloadType": "T"
    },
    {
      "name": "onPageChange",
      "payloadType": "number"
    }
  ],
  "slots": [],
  "bestPractices": [
    "Keep tables readable: enable `striped` and/or `hoverable` for large datasets, but avoid heavy styling that competes with content.",
    "Prefer smart height over tiny scrollbars: enable `constrainHeight` and set `minConstrainedRows` (default 4). If the viewport would show fewer rows, the table expands; when the dataset exceeds `maxExpandedRows` (default 30), it paginates instead of showing a tiny scroller.",
    "Provide an initial sort (`initialSort`), align numeric columns to the right, and format dates consistently.",
    "Enable selection only when follow‑up actions operate on selected rows; use the unified `selectionMode` prop (`'none' | 'single' | 'multiple'`) to match the action design. The pre‑0.35 `selectable` prop (`'single' | 'multi'`) is a deprecated alias that still works but dev‑warns once and is removed at 1.0 — migrate `selectable='multi'` to `selectionMode='multiple'`.",
    "When the table is selectable and the data is refreshed immutably (new row objects fetched or re‑mapped), set `getItemKey` (a property name like `getItemKey=\"id\"` or a function `getItemKey={(row) => row.id}`) so selection is tracked by a stable key and survives the refresh. Without it, selection falls back to object identity and is cleared whenever the row objects are replaced. The pre‑0.35 `rowKey` prop is a deprecated alias of `getItemKey` (same shape; removed at 1.0).",
    "Keep column count reasonable on narrow screens; prefer fewer, more meaningful columns over many cramped ones. Use succinct headers.",
    "Expose `onRowClick` when rows drive navigation or drill‑ins so the parent surface can react without wiring bespoke click handlers into each cell."
  ],
  "examples": [
    {
      "id": "table-basic",
      "title": "Basic striped table",
      "code": "(() => {\n  type Row = { id: number; name: string; city: string };\n  const columns: TableColumn<Row>[] = [\n    { header: 'ID', accessor: 'id', align: 'right' },\n    { header: 'Name', accessor: 'name' },\n    { header: 'City', accessor: 'city' },\n  ];\n  const data: Row[] = [\n    { id: 1, name: 'Ada Lovelace', city: 'London' },\n    { id: 2, name: 'Alan Turing', city: 'Manchester' },\n    { id: 3, name: 'Grace Hopper', city: 'New York' },\n  ];\n  return (<Panel fullWidth><Table data={data} columns={columns} striped hoverable constrainHeight /></Panel>);\n})()",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {
        "data": [],
        "columns": []
      }
    },
    {
      "id": "table-selectable",
      "title": "Single‑select with initial sort",
      "code": "(() => {\n  type Row = { id: number; name: string; age: number };\n  const columns: TableColumn<Row>[] = [\n    { header: 'ID', accessor: 'id', align: 'right', sortable: true },\n    { header: 'Name', accessor: 'name', sortable: true },\n    { header: 'Age', accessor: 'age', align: 'right', sortable: true },\n  ];\n  const data: Row[] = [\n    { id: 1, name: 'Katherine Johnson', age: 41 },\n    { id: 2, name: 'Tim Berners‑Lee', age: 35 },\n    { id: 3, name: 'Linus Torvalds', age: 29 },\n  ];\n  const Demo = () => {\n    const [selected, setSelected] = React.useState<Row[]>([]);\n    return (<Stack>\n      <Panel fullWidth>\n        <Table data={data} columns={columns} selectionMode='single' initialSort={{ index: 0 }} onSelectionChange={setSelected} />\n      </Panel>\n      <Typography variant='subtitle'>Selected: {selected.map((r) => r.id).join(', ') || '—'}</Typography>\n    </Stack>);\n  };\n  return <Demo />;\n})()",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {
        "data": [],
        "columns": []
      }
    },
    {
      "id": "table-rowkey-selection",
      "title": "Selection that survives a data refresh (getItemKey)",
      "code": "(() => {\n  type Row = { id: number; name: string };\n  const columns: TableColumn<Row>[] = [\n    { header: 'ID', accessor: 'id', align: 'right' },\n    { header: 'Name', accessor: 'name' },\n  ];\n  const seed: Row[] = [\n    { id: 1, name: 'Ada Lovelace' },\n    { id: 2, name: 'Alan Turing' },\n    { id: 3, name: 'Grace Hopper' },\n  ];\n  const Demo = () => {\n    const [data, setData] = React.useState<Row[]>(seed);\n    const [selected, setSelected] = React.useState<Row[]>([]);\n    // Immutable refresh: brand-new row objects, same ids. With getItemKey='id'\n    // the current selection is preserved across the refresh.\n    const refresh = () => setData((d) => d.map((r) => ({ ...r })));\n    return (\n      <Stack gap={2}>\n        <Panel fullWidth>\n          <Table data={data} columns={columns} selectionMode='multiple' getItemKey='id' onSelectionChange={setSelected} />\n        </Panel>\n        <Button onClick={refresh}>Refresh data</Button>\n        <Typography variant='subtitle'>Selected: {selected.map((r) => r.id).join(', ') || '—'}</Typography>\n      </Stack>\n    );\n  };\n  return <Demo />;\n})()",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {
        "data": [],
        "columns": []
      }
    },
    {
      "id": "table-selection-mode",
      "title": "Unified selection vocabulary (selectionMode + getItemKey)",
      "code": "(() => {\n  type Row = { id: number; name: string };\n  const columns: TableColumn<Row>[] = [\n    { header: 'ID', accessor: 'id', align: 'right' },\n    { header: 'Name', accessor: 'name' },\n  ];\n  const data: Row[] = [\n    { id: 1, name: 'Ada Lovelace' },\n    { id: 2, name: 'Alan Turing' },\n    { id: 3, name: 'Grace Hopper' },\n  ];\n  const Demo = () => {\n    const [selected, setSelected] = React.useState<Row[]>([]);\n    return (\n      <Stack gap={2}>\n        <Panel fullWidth>\n          <Table data={data} columns={columns} selectionMode='multiple' getItemKey='id' onSelectionChange={setSelected} />\n        </Panel>\n        <Typography variant='subtitle'>Selected: {selected.map((r) => r.id).join(', ') || '—'}</Typography>\n      </Stack>\n    );\n  };\n  return <Demo />;\n})()",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {
        "data": [],
        "columns": []
      }
    },
    {
      "id": "table-auto-pagination",
      "title": "Auto pagination via minConstrainedRows",
      "code": "(() => {\n  type Row = { id: number; name: string; city: string };\n  const columns: TableColumn<Row>[] = [\n    { header: 'ID', accessor: 'id', align: 'right' },\n    { header: 'Name', accessor: 'name' },\n    { header: 'City', accessor: 'city' },\n  ];\n  const data: Row[] = Array.from({ length: 60 }, (_, i) => ({ id: i + 1, name: 'User ' + (i + 1), city: 'City ' + (i % 8) }));\n  return (\n    <Panel fullWidth>\n      <Table\n        data={data}\n        columns={columns}\n        striped\n        hoverable\n        constrainHeight\n        minConstrainedRows={4}\n        maxExpandedRows={20}\n      />\n    </Panel>\n  );\n})()",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {
        "data": [],
        "columns": []
      }
    },
    {
      "id": "table-force-pagination",
      "title": "Force pagination with page size",
      "code": "(() => {\n  type Row = { id: number; name: string; city: string };\n  const columns: TableColumn<Row>[] = [\n    { header: 'ID', accessor: 'id', align: 'right' },\n    { header: 'Name', accessor: 'name' },\n    { header: 'City', accessor: 'city' },\n  ];\n  const data: Row[] = Array.from({ length: 48 }, (_, i) => ({ id: i + 1, name: 'Row ' + (i + 1), city: 'Metro ' + (i % 5) }));\n  const Demo = () => {\n    const [page, setPage] = React.useState(1);\n    return (\n      <Panel fullWidth>\n        <Table\n          data={data}\n          columns={columns}\n          striped\n          paginate\n          maxExpandedRows={12}\n          page={page}\n          onPageChange={setPage}\n          paginationWindow={7}\n        />\n      </Panel>\n    );\n  };\n  return <Demo />;\n})()",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {
        "data": [],
        "columns": []
      }
    },
    {
      "id": "table-row-click",
      "title": "Row click actions",
      "code": "(() => {\n  type Invoice = { id: number; customer: string; total: number };\n  const columns: TableColumn<Invoice>[] = [\n    { header: 'Invoice', accessor: 'id', align: 'right' },\n    { header: 'Customer', accessor: 'customer' },\n    {\n      header: 'Total',\n      accessor: (row: Invoice) => `$${row.total.toFixed(2)}`,\n      align: 'right',\n    },\n  ];\n  const data: Invoice[] = [\n    { id: 4821, customer: 'Juniper Labs', total: 8421.17 },\n    { id: 4822, customer: 'Halo Forge', total: 311.25 },\n    { id: 4823, customer: 'Even Tide', total: 1775.4 },\n  ];\n  const Demo = () => {\n    const [lastClick, setLastClick] = React.useState<Invoice | null>(null);\n    return (\n      <Stack gap={2}>\n        <Panel fullWidth>\n          <Table\n            data={data}\n            columns={columns}\n            hoverable\n            onRowClick={(row) => setLastClick(row)}\n          />\n        </Panel>\n        <Typography variant='subtitle'>\n          Last clicked: {lastClick ? `${lastClick.customer} (#${lastClick.id})` : '—'}\n        </Typography>\n      </Stack>\n    );\n  };\n  return <Demo />;\n})()",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {
        "data": [],
        "columns": []
      }
    }
  ],
  "docsUrl": "/table-demo",
  "sourceFiles": [
    "src/components/widgets/Table.tsx",
    "docs/src/pages/components/widgets/TableDemo.tsx"
  ],
  "version": "0.36.0",
  "schemaVersion": "1.7"
}