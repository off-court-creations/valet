{
  "name": "Snackbar",
  "category": "widgets",
  "slug": "components/widgets/snackbar",
  "summary": "Snackbar component",
  "status": "experimental",
  "aliases": [
    "snackbar",
    "toast",
    "notification",
    "alert"
  ],
  "props": [
    {
      "name": "open",
      "type": "boolean",
      "required": false
    },
    {
      "name": "onClose",
      "type": "() => void",
      "required": false
    },
    {
      "name": "autoHideDuration",
      "type": "number | null",
      "required": false,
      "default": "4000"
    },
    {
      "name": "message",
      "type": "React.ReactNode",
      "required": false
    },
    {
      "name": "noStack",
      "type": "boolean",
      "required": false,
      "default": "false"
    },
    {
      "name": "sx",
      "type": "Sx",
      "required": false
    }
  ],
  "domPassthrough": {
    "element": "div",
    "omitted": [
      "style"
    ]
  },
  "cssVars": [],
  "cssPresets": [],
  "events": [
    {
      "name": "onClose"
    }
  ],
  "actions": [],
  "slots": [
    {
      "name": "message"
    }
  ],
  "bestPractices": [
    "Keep messages short and specific. Snackbars confirm actions or provide brief status; avoid long instructions or multi‑step interactions.",
    "Place them consistently, typically bottom center/right depending on platform.",
    "Offer a single, clear action when needed; prefer undo when appropriate.",
    "Respect motion and timing. Use `autoHideDuration` for transient messages (3–6s typical); longer durations should be deliberate.",
    "Don’t stack excessively. Queue messages when necessary; ensure important ones aren’t missed.",
    "Keep focus behavior intact. Snackbars shouldn’t steal focus; any action button must be keyboard reachable.",
    "Style via tokens/presets. Use theme colors for status (e.g., `primary` / `error`) and consolidate variants with `preset`."
  ],
  "examples": [],
  "sourceFiles": [
    "src/components/widgets/Snackbar.tsx"
  ],
  "version": "0.32.0",
  "schemaVersion": "1.6"
}