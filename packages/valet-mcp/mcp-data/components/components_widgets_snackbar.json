{
  "name": "Snackbar",
  "category": "widgets",
  "slug": "components/widgets/snackbar",
  "summary": "Transient toast notification (role='status') that fades in and auto-dismisses, pausing its timer on hover/focus; control with `open`/`message`/`autoHideDuration`, and pick a `variant` (`outline` default or solid `filled`) — use it for brief, non-blocking feedback.",
  "status": "stable",
  "aliases": [
    "snackbar",
    "toast",
    "notification",
    "alert"
  ],
  "props": [
    {
      "name": "open",
      "type": "boolean",
      "required": false,
      "description": "Controlled open state – omit for uncontrolled"
    },
    {
      "name": "onClose",
      "type": "() => void",
      "required": false,
      "description": "Called when the snackbar has fully hidden"
    },
    {
      "name": "autoHideDuration",
      "type": "number | null",
      "required": false,
      "description": "Auto-hide after N ms – `null` disables (default = 4000 ms)",
      "default": "4000"
    },
    {
      "name": "message",
      "type": "React.ReactNode",
      "required": false,
      "description": "Convenience message when no children supplied"
    },
    {
      "name": "noStack",
      "type": "boolean",
      "required": false,
      "description": "Disable the internal flex stack (children render 1:1)",
      "default": "false"
    },
    {
      "name": "variant",
      "type": "SnackbarVariant",
      "required": false,
      "description": "Visual style — `outline` (default) or solid `filled`.",
      "default": "'outline'"
    },
    {
      "name": "sx",
      "type": "Sx",
      "required": false,
      "description": "Inline styles (with CSS var support)"
    },
    {
      "name": "preset",
      "type": "string | string[] | undefined",
      "required": false,
      "description": "One or many style-preset names registered via `definePreset()`"
    }
  ],
  "domPassthrough": {
    "element": "div",
    "omitted": [
      "style"
    ]
  },
  "cssVars": [],
  "cssPresets": [],
  "events": [
    {
      "name": "onClose"
    }
  ],
  "slots": [
    {
      "name": "message"
    }
  ],
  "bestPractices": [
    "Keep messages short and specific. Snackbars confirm actions or provide brief status; avoid long instructions or multi‑step interactions.",
    "Place them consistently, typically bottom center/right depending on platform. On mobile the bottom offset automatically clears the home indicator / gesture bar (`env(safe-area-inset-bottom)`), and any action button you nest should be a real touch target (a valet `Button`/`IconButton`, which floor at ≥44px on touch).",
    "Pick a `variant`: `outline` (default) reads as a quiet card with a primary keyline; `filled` is a solid primary surface with contrasting text + a soft shadow for more emphasis. Reserve `filled` for messages that should stand out.",
    "Offer a single, clear action when needed; prefer undo when appropriate.",
    "Respect motion and timing. Use `autoHideDuration` for transient messages (3–6s typical); longer durations should be deliberate. The countdown pauses while the snackbar is hovered or focused and resumes with the remaining time. The slide/fade transition is automatically suppressed for users who request reduced motion.",
    "For urgent errors, override the default live region with `role='alert'`/`aria-live='assertive'` so it is announced immediately.",
    "Don’t rely on queueing — one Snackbar shows one message at a time; manage your own queue/state if several messages can overlap.",
    "Keep focus behavior intact. Snackbars shouldn’t steal focus; any action button must be keyboard reachable.",
    "Style via tokens/presets. Use theme colors for status (e.g., `primary` / `error`) and consolidate variants with `preset`."
  ],
  "examples": [],
  "docsUrl": "/snackbar-demo",
  "sourceFiles": [
    "src/components/widgets/Snackbar.tsx",
    "docs/src/pages/components/widgets/SnackbarDemo.tsx"
  ],
  "version": "0.36.0",
  "schemaVersion": "1.7"
}