{
  "name": "Slider",
  "category": "fields",
  "slug": "components/fields/slider",
  "summary": "Theme-aware draggable slider for selecting a number within `min`/`max` (optionally snapped to `step`); use it for continuous or coarse-grained ranges where the approximate value matters more than an exact typed number.",
  "status": "stable",
  "aliases": [
    "slider",
    "range"
  ],
  "props": [
    {
      "name": "value",
      "type": "number",
      "required": false,
      "description": "Controlled value."
    },
    {
      "name": "defaultValue",
      "type": "number",
      "required": false,
      "description": "Default for uncontrolled usage."
    },
    {
      "name": "onValueChange",
      "type": "OnValueChange<number>",
      "required": false,
      "description": "Canonical value change (pointer drag, keyboard)."
    },
    {
      "name": "onValueCommit",
      "type": "OnValueCommit<number>",
      "required": false,
      "description": "Commit event (pointer up, blur, or Enter/Page/Home/End key)."
    },
    {
      "name": "min",
      "type": "number",
      "required": false,
      "description": "Minimum allowed value."
    },
    {
      "name": "max",
      "type": "number",
      "required": false,
      "description": "Maximum allowed value."
    },
    {
      "name": "step",
      "type": "number",
      "required": false,
      "description": "Fixed increment when `snap=\"step\"`; defaults to 1."
    },
    {
      "name": "presets",
      "type": "number[]",
      "required": false,
      "description": "Preset snap points when `snap=\"presets\"`."
    },
    {
      "name": "snap",
      "type": "SnapMode",
      "required": false,
      "description": "Snap behaviour."
    },
    {
      "name": "precision",
      "type": "number",
      "required": false,
      "description": "Number of decimal places allowed. Default 0 (integers only)."
    },
    {
      "name": "showValue",
      "type": "boolean",
      "required": false,
      "description": "Show live numeric value above thumb."
    },
    {
      "name": "showMinMax",
      "type": "boolean",
      "required": false,
      "description": "Show min/max labels beneath the track ends."
    },
    {
      "name": "showTicks",
      "type": "boolean",
      "required": false,
      "description": "Show tick marks (follows snap points or `ticks`)."
    },
    {
      "name": "ticks",
      "type": "number[]",
      "required": false,
      "description": "Custom tick set (overrides derived ticks)."
    },
    {
      "name": "size",
      "type": "SliderSize | number | string",
      "required": false
    },
    {
      "name": "disabled",
      "type": "boolean",
      "required": false
    },
    {
      "name": "name",
      "type": "string | undefined",
      "required": false,
      "description": "Field identifier used for FormControl binding and forwarded to the underlying control\nwhen applicable. Components can require `name` locally if they depend on it."
    },
    {
      "name": "label",
      "type": "React.ReactNode",
      "required": false,
      "description": "Visual label rendered by the component or its wrapper. Accepts string or node."
    },
    {
      "name": "helperText",
      "type": "React.ReactNode",
      "required": false,
      "description": "Supplemental hint or validation content typically rendered under the control."
    },
    {
      "name": "error",
      "type": "boolean | undefined",
      "required": false,
      "description": "Marks the field invalid; components may adjust visuals and helper text color."
    },
    {
      "name": "fullWidth",
      "type": "boolean | undefined",
      "required": false,
      "description": "Stretches the field wrapper to occupy the full available width."
    },
    {
      "name": "sx",
      "type": "Sx | undefined",
      "required": false,
      "description": "Inline styles (with CSS var support)."
    },
    {
      "name": "preset",
      "type": "string | string[] | undefined",
      "required": false,
      "description": "One or many style-preset names registered via `definePreset()`"
    }
  ],
  "domPassthrough": {
    "element": "div",
    "omitted": [
      "onChange",
      "style"
    ]
  },
  "cssVars": [
    "--valet-bg",
    "--valet-text-color"
  ],
  "cssPresets": [],
  "events": [
    {
      "name": "onValueChange",
      "payloadType": "OnValueChange<number>"
    },
    {
      "name": "onValueCommit",
      "payloadType": "OnValueCommit<number>"
    }
  ],
  "slots": [
    {
      "name": "label"
    },
    {
      "name": "helperText"
    }
  ],
  "bestPractices": [
    "Use Slider for ranges where relative position matters; prefer `TextField` for precise numeric entry.",
    "Snap deliberately. Provide step increments that match the domain; avoid overly fine steps that make keyboard or touch fiddly.",
    "Label min/max or current value. Show ticks or helper text for context; keep labels short and avoid clutter.",
    "Keyboard support. Ensure Left/Right (or Up/Down) adjust values by one step, and Page/Home/End jump predictably.",
    "Size for touch. Pick token sizes or numeric height to keep the thumb easy to grab on mobile.",
    "Use presets/tokens for colour and shape. Keep theme‑aligned styling rather than per‑instance CSS.",
    "Binding precedence (prop > form > internal). An explicit `value` prop always wins; otherwise a `name` inside a `FormControl` binds to the store (seed the key in `createFormStore(initial)` so the initial value is honoured); otherwise the Slider is uncontrolled. The binding is latched at mount — don't switch a Slider between controlled and uncontrolled.",
    "Change source. `onValueChange`/`onValueCommit` report a truthful `info.source`: `'pointer'` for a drag/click on the track and `'keyboard'` for the arrow/Page/Home/End keys, not a blanket `'programmatic'`."
  ],
  "examples": [
    {
      "id": "slider-controlled",
      "title": "Controlled slider",
      "code": "() => {\n  const [volume, setVolume] = React.useState(40);\n  return (<Stack gap={1}>\n    <Slider min={0} max={100} step={5} value={volume} onValueChange={(next) => setVolume(next)} showValue showMinMax />\n    <Typography>Volume: {volume}</Typography>\n  </Stack>);\n}",
      "lang": "tsx",
      "runnable": true,
      "minimalProps": {}
    }
  ],
  "docsUrl": "/slider-demo",
  "sourceFiles": [
    "src/components/fields/Slider.tsx",
    "docs/src/pages/components/field/SliderDemo.tsx"
  ],
  "version": "0.35.0",
  "schemaVersion": "1.7"
}