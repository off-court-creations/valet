{
  "name": "Progress",
  "category": "primitives",
  "slug": "components/primitives/progress",
  "summary": "Progress component",
  "status": "stable",
  "aliases": [
    "progress",
    "spinner",
    "loader"
  ],
  "props": [
    {
      "name": "variant",
      "type": "ProgressVariant",
      "required": false,
      "description": "Prefer ProgressBar/ProgressRing. kept for back‑compat."
    },
    {
      "name": "mode",
      "type": "ProgressMode",
      "required": false,
      "description": "Prefer `value` unset to indicate indeterminate."
    },
    {
      "name": "value",
      "type": "number",
      "required": false,
      "description": "0–100"
    },
    {
      "name": "buffer",
      "type": "number",
      "required": false,
      "description": "0–100 buffer for linear"
    },
    {
      "name": "size",
      "type": "number | string",
      "required": false,
      "description": "Ring diameter (or legacy token)."
    },
    {
      "name": "thickness",
      "type": "number | string",
      "required": false,
      "description": "Bar height or ring thickness."
    },
    {
      "name": "showLabel",
      "type": "boolean",
      "required": false,
      "description": "Deprecated: use `label` on ProgressRing or provide children."
    },
    {
      "name": "color",
      "type": "string",
      "required": false,
      "description": "Override primary color."
    },
    {
      "name": "children",
      "type": "React.ReactNode",
      "required": false,
      "description": "Center content for ring."
    },
    {
      "name": "sx",
      "type": "Sx",
      "required": false,
      "description": "Inline styles (supports CSS vars)."
    },
    {
      "name": "preset",
      "type": "string | string[] | undefined",
      "required": false,
      "description": "One or many style-preset names registered via `definePreset()`"
    }
  ],
  "domPassthrough": {
    "element": "div",
    "omitted": [
      "style"
    ]
  },
  "cssVars": [
    "--valet-progress-radius",
    "--valet-text-color"
  ],
  "cssPresets": [],
  "events": [],
  "actions": [],
  "slots": [
    {
      "name": "children"
    }
  ],
  "bestPractices": [
    "Use determinate bars when percent is known; spinners for unknown durations.",
    "Provide accessible labels (e.g., `aria-label` or nearby text) that explain what is loading.",
    "Avoid blocking the entire page—prefer inline or section‑level indicators when possible.",
    "Use theme colours to convey status (e.g., `error` for failures) and tokens for size.",
    "Don’t rely solely on colour; pair with text or icons for critical states.",
    "Keep motion subtle and aligned with the theme’s motion tokens."
  ],
  "examples": [],
  "sourceFiles": [
    "src/components/primitives/Progress.tsx"
  ],
  "version": "0.34.0",
  "schemaVersion": "1.6"
}