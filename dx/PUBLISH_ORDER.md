# Publish Order — moved

> **This file is a tombstone.** The release runbook now lives in
> [`dx/RELEASING.md`](./RELEASING.md), where **every gate is mandatory and
> uncommented** (preflight quality gates, CHANGELOG discipline, pin updates,
> publish order, post-publish verification, the SECURITY.md row, branch
> protection, and the 0.34.2 hotfix path).
>
> The old "publish order" content lived here with commented-out validators
> (e.g. `# npm run cva:validate`) and no enforced gate. Do not use it; follow
> `dx/RELEASING.md` instead.

See **[dx/RELEASING.md](./RELEASING.md)**.
