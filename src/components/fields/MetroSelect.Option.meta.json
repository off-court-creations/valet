{
  "name": "MetroSelect.Option",
  "summary": "A single tile inside a `MetroSelect`, identified by `value` with icon/label children; author one per choice as a direct `MetroSelect` child — the parent owns selection state.",
  "status": "experimental"
}
