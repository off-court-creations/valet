{
  "name": "RadioGroup",
  "summary": "Single-choice group of `Radio` options with keyboard roving focus; controlled via `value` + `onValueChange` or bound to a `FormControl` by `name`, with `gap` for option spacing (the old `spacing` prop is a deprecated alias).",
  "status": "stable",
  "aliases": ["radiogroup", "radio-group", "radio-buttons"]
}
